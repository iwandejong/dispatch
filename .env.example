# Copy to .env (or run ./scripts/init-env.sh, which generates this for you). Never commit .env.

# Postgres password. The database is only reachable inside the compose network.
POSTGRES_PASSWORD=change-me
